# 実装計画

- [x] 1. (P) useGeolocationフックの実装
  - ブラウザのGeolocation APIをラップするカスタムフックを作成する
  - 位置取得中のローディング状態、成功時の座標、失敗時のエラーメッセージを状態として管理する
  - エラーコード（PERMISSION_DENIED、POSITION_UNAVAILABLE、TIMEOUT）に応じた日本語エラーメッセージを返す
  - Geolocation API非対応環境では即座にエラーを返す
  - locate関数を公開し、呼び出し元から位置取得をトリガー可能にする
  - _Requirements: 1.2, 1.3, 4.1, 4.2_

- [x] 2. UIコンポーネントと地図表示の拡張
- [x] 2.1 (P) 現在位置マーカーコンポーネントを作成する
  - 現在位置を示す青い円マーカーを地図上に表示するコンポーネントを作成する
  - 店舗マーカー（Pinコンポーネント）とは視覚的に明確に区別可能にする
  - AdvancedMarkerにカスタムHTML要素を渡し、パルスアニメーションを付与する
  - _Requirements: 2.1, 2.3_

- [x] 2.2 (P) 現在位置取得ボタンコンポーネントを作成する
  - 地図右下にFABスタイルの現在位置取得ボタンを配置する
  - ローディング中はスピナーを表示しボタンを無効化する
  - エラー発生時はボタン付近にエラーメッセージを一定時間表示する
  - タッチターゲットは44px以上を確保する
  - _Requirements: 1.1, 4.1, 4.2_

- [x] 2.3 MapViewに現在位置への地図移動機能を追加する
  - MapViewにuserLocationプロップを追加し、現在位置座標を受け取れるようにする
  - userLocationが設定された時、useMapフックを使い地図の中心を現在位置に移動する
  - 現在位置付近の店舗が視認できるズームレベル（14）に調整する
  - userLocationがある場合のみCurrentLocationMarkerを描画する
  - _Requirements: 2.2, 3.1_

- [x] 3. MapPageへの統合とビルド検証
  - MapPageにuseGeolocationフックを統合し、位置取得状態を管理する
  - LocateButtonを配置し、位置取得トリガーとエラー表示を接続する
  - 取得した座標をMapViewのuserLocationプロップに渡す
  - 位置情報取得なしでも既存機能（地図表示・フィルタリング）が正常動作することを確認する
  - npm run buildでビルドが成功し、既存テストにリグレッションがないことを確認する
  - _Requirements: 1.3, 3.2, 4.3_
